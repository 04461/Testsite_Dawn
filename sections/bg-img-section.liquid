<section class="section-class-2">
  <div class="img-2">
    <img class="bg-img" src="{{ section.settings.image | img_url:"master" }}" alt="">
  </div>
  <div class="inner-main">
    <h2>
      {{ section.settings.heading }}
    </h2>
    <div class="shipping-2 wrapper test_542184185">
      {% for block in section.blocks %}
        {% if block.type == 'column' %}
          {% if block.settings.url != blank %}
          {% endif %}

          <div style="text-align: center;" class="shipping-2">
            <p class="title-2">
              {{ block.settings.title }}
            </p>

            <p class="desc-2">
              {{ block.settings.description }}
            </p>
          </div>
          {% if block.settings.url != blank %}
          {% endif %}
        {% endif %}
      {% endfor %}
    </div>

    <style>
                    @media screen and (max-width: 768px) {
                 .img-2 img{
                 height: 521px;
                }

                    @media screen and (max-width: 768px) {
                 .inner-main h2{
                  font-size:15px !important;
            /*        width:20px !important;
                   height:15px !important; */
                }
                }
                   @media screen and (max-width: 768px) {
                 .shipping-2.wrapper{
                     width: 278px;
                     flex-direction: column;
            }
                }
                   @media screen and (max-width: 768px) {
                 .shipping-2{
                     width: 100% !important;

            }
                }


      {% comment %}              @media screen and (max-width: 768px) {
                 .shipping-2.wrapper{
                   margin-left: 13px ;
                }
                } {% endcomment %}
    </style>

    <h3 class="form_style">
      {{ section.settings['form-heading'] }}
    </h3>
    <div>
      {% form 'customer', class: 'newsletter-form' %}
        <input type="hidden" name="contact[tags]" value="newsletter">
        <div class="newsletter-form__field-wrapper">
          <div class="field">
            <input
              id="NewsletterForm--{{ section.id }}"
              type="email"
              name="contact[email]"
              class="field__input"
              value="{{ form.email }}"
              aria-required="true"
              autocorrect="off"
              autocapitalize="off"
              autocomplete="email"
              {% if form.errors %}
                autofocus
                aria-invalid="true"
                aria-describedby="Newsletter-error--{{ section.id }}"
              {% elsif form.posted_successfully? %}
                aria-describedby="Newsletter-success--{{ section.id }}"
              {% endif %}
              placeholder="{{ 'newsletter.label' | t }}"
              required
            >
            <label class="field__label" for="NewsletterForm--{{ section.id }}">
              {{ 'newsletter.label' | t }}
            </label>
            <button
              type="submit"
              class="newsletter-form__button field__button"
              name="commit"
              id="Subscribe"
              aria-label="{{ 'newsletter.button_label' | t }}"
            >
              {% render 'icon-arrow' %}
            </button>
          </div>
          {%- if form.errors -%}
            <small class="newsletter-form__message form__message" id="Newsletter-error--{{ section.id }}">
              {%- render 'icon-error' -%}
              {{- form.errors.translated_fields.email | capitalize }}
              {{ form.errors.messages.email -}}
            </small>
          {%- endif -%}
        </div>
        {%- if form.posted_successfully? -%}
          <h3
            class="newsletter-form__message newsletter-form__message--success form__message"
            id="Newsletter-success--{{ section.id }}"
            tabindex="-1"
            autofocus
          >
            {% render 'icon-success' -%}
            {{- 'newsletter.success' | t }}
          </h3>
        {%- endif -%}
      {% endform %}
    </div>
  </div>
</section>

<style>
    #shopify-section-{{section.id}} .form_style {
      color:white;
      text-align: center;
      text-align: center;
      width: 100%;
      max-width: 720px;
      margin-inline: auto;
      font-size: 20px;
  }

  #shopify-section-{{section.id}} .shipping-2.wrapper{
      display: flex;
      width: 100%;
      max-width: 700px;
      margin-inline: auto;

  }
    #shopify-section-{{section.id}} .shipping-2 .title-2{
      color:black;
      background-color:white;
      width: 45px;
      height: 45px;
      justify-content: center;
      display: inline-flex;
      align-items: center;
      border-radius: 50%;
    }

    #shopify-section-{{section.id}} .inner-main{
       position: absolute;
      top: 0;
      bottom: 0;
      left: 0;
      right: 0;
      z-index: 2;
      padding-top: 63px;
    }
   #shopify-section-{{section.id}} .section-class-2{
      position:relative;
      width: 100%;
      max-width: 1000px;
      margin-inline: auto;
      padding-top: 60px;
      overflow:hidden;
   }

     #shopify-section-{{section.id}} .shipping-2{
      text-align: center;
      width: 33.3%;
  }
   #shopify-section-{{section.id}} .inner-main h2{
      color:white;
      text-align: center;
      text-align: center;
      width: 100%;
      max-width: 720px;
      margin-inline: auto;
      font-size: 48px;
    }

    #shopify-section-{{section.id}} .form_style h3{
       color:white;
      text-align: center;
      text-align: center;
      width: 100%;
      max-width: 720px;
      margin-inline: auto;
      font-size: 20px;
    }
  #shopify-section-{{section.id}} .img-2 img{
    width:100%;
    max-width:100%;
  }
   #shopify-section-{{section.id}} .logo-list-section{
     overflow:hidden;
   }


   #shopify-section-{{section.id}} .shipping-2 .desc-2{
      max-width: 63%;
      margin-left: auto;
      margin-right: auto;
      color: white;
  }

    #shopify-section{{section.id}} .shipping .shipping-2{
      display:flex;

  }
         @media screen and (max-width: 768px) {
     .inner-main h2{
           font-size: 13px;
    }
    }
        @media screen and (max-width: 768px) {
     .shipping-2 {
       display: flex;
       margin-left: 7px;
       overflow:hidden;
      }
      }
</style>

{% schema %}
  {
   "name": "3 Block section",
   "settings": [
       {
         "type": "image_picker",
         "id": "image",
         "label": "Background Image"
       },

{
         "type": "text",
         "id": "heading",
         "label": "Heading"
       },
     {
         "type": "text",
         "id": "form-heading",
         "label": "Form Heading"
       }

   ],

     "blocks": [
   {
     "type": "column",
     "name": "t:sections.multicolumn.blocks.column.name",
     "settings": [

        {
         "type": "text",
         "id": "title",
         "label": "t:sections.multicolumn.blocks.column.settings.title.label"
       },
       {
         "type": "inline_richtext",
         "id": "description",
         "label": "Description"
       },

        {
         "type": "color",
         "label" :"Background Color",
         "id": "bg_color"
       }

     ]
   }
 ],

   "presets": [
   {
     "name": "3 Block section"

   }
 ]
 }
{% endschema %}

{% stylesheet %}
{% endstylesheet %}

{% javascript %}
{% endjavascript %}
